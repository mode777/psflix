# PocketBase instance URL for the SPA (build-time). When unset, dev builds
# target http://127.0.0.1:8090 and production builds use same-origin.
VITE_PB_URL=

# Optional: a superuser token for one-off scripts. Never commit a real value.
# PB_SUPERUSER_TOKEN=